rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

หมายเหตุ: Rules นี้บังคับว่าต้อง Login Firebase Authentication ก่อน จึงอ่าน/เขียนข้อมูลได้
